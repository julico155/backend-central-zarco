node_modules/
dist/
coverage/
/storage/
.claude/
.env
.env.*
!.env.example
*.log
.DS_Store

# Colección de Postman para probar la API del banco directo — el
# environment trae credenciales reales de certificación, nunca al repo.
postman/baneco-certificacion/

# Reporte de introspección de la DB Agente (metadatos; no se versiona)
agent-schema-report*.txt
central-schema-report*.txt

# E2E contra DB reales: manifest local de ids creados
e2e-manifest.json*

# E2E contra DB reales: manifest local de ids creados
e2e-manifest.json*
